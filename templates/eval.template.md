---
specId: EVAL-<组名>-001
type: eval
title: 企业级高可用多智能体客服 AI 智能体系统
parent_spec: spec.md
plan_ref: plan.md
tasks_ref: tasks.md
domain: 客服
system: <组名>-customer-service-agent
owner: <组名 / 组员姓名>
subject:
  repo:
  branch:
  env: local demo
verdict:                # 通过 / 有条件通过 / 不通过
verdict_by:
verdict_at:
status: 草稿             # 草稿（先写用例）→ 已实测（回填结果）
confirmed: false
version: 0.1
updated_at:
knowledge_refs: []
related_specs: [learnings.md]
score: {}
---

<!-- ──────────────────────────────────────────────────────────────
  eval.md ｜ 验收判定（先定标准，再编码；编码后回填实测结果）

  谁写、谁评审、从哪来      这份文件不是单一来源——§0、§1（AC 覆盖
        矩阵 + §1.1 测试用例）、§2（验证范围）、§4（非功能门槛）只
        依赖 spec.md（§5 的 US 编号、§3 的场景范围、§6 的性能/准确率
        指标），Coding Agent 在 spec.md 确认后就能起草，跟 plan.md 是
        并行的两条分支，不用等它。但 §3（系统级回归确认）和 §5（不改
        清单核对）结构上依赖 plan.md：§5 直接核对的就是 plan.md §6
        的"不改清单"，没有 plan.md 无从写起；§3 的"期望降级行为"具体
        是什么话术、走不走重试，取决于 plan.md §5 的降级设计，spec.md
        只说了"要有兜底"，没说兜底长什么样。plan.md 确认后（不用等到
        编码完成）就可以让 Coding Agent 补上这两节。tasks.md 则完全
        不是 eval.md 的依赖，哪一节都不需要它。

        人评审、纠错 + 补充后确认，改动记进 §8 变更记录。评审 §0/§1
        时重点看测试用例是不是每条 US 都真的对应到了具体语句；评审
        §3/§5 时重点核对是不是真的照着 plan.md 抄对了，而不是自己
        编的。

  为什么叫"系统级回归确认"而不是"下游回归确认"      本次是从零
        搭建的新系统，没有真实的下游依赖方，§3 关注的是系统内部
        （高可用 / 降级路径）是否覆盖，而不是外部下游是否受影响。

  为什么分两次写      §0、§1、§2、§4 在 spec.md 确认后就起草完成并
        评审确认；§3、§5 在 plan.md 确认后补齐；§6、§7 要等代码写完、
        真正跑一遍测试才能填。没有先定好的验收标准，"做完了"就只能
        靠肉眼感觉判断。

  §7 判定结论谁签      Coding Agent 可以先给出建议结论，但 verdict_by
        要填人名——这是编码完成后唯一还留着的人工把关点，判定"通过"
        或"有条件通过"之后才进入部署发布。

  这份文件是 learnings.md 的重点素材来源      别的文档主要贡献
        "评审时改了什么"，eval.md 还额外贡献"实际测出来什么"：
        §3/§4 里没通过的门禁、§6 的遗留问题、§7 判定"有条件通过"
        的理由，往往比评审阶段的纠错更能说明这次哪里做得不够。
        所以这几节即使结论不好看也要如实填，不要为了让判定通过而
        把没测到的写成测过了——那样 learnings.md 就只剩空话。
────────────────────────────────────────────────────────────── -->

# EVAL-\<组名\>-001｜验收判定

## 0. 为什么需要这份文件

一句话：怎么证明这次交付通过了。每一节都指回 spec.md 的某条 US 编号或 prd.md 的某条验收标准，不是凭空加的检查项。

## 1. AC 覆盖矩阵（门禁一：覆盖率必须 100%）

对照 spec.md §5 的每一条 US，逐条列出是否被测试用例覆盖。"对应 prd §4"这一列用来反查 prd.md §4 的五条验收标准有没有漏——**§4-1 到 §4-5 每条都必须在这列出现过**：

| US 编号 | 对应 prd §4 | 测试用例 # / 验证方式 | 覆盖 |
|---|---|---|---|
| US-01 | §4-1 基准场景 | | ⬜ |
| US-02 | §4-2 每类 ≥3 场景 | | ⬜ |
| US-03 | §4-4 高可用兜底 | | ⬜ |
| US-04 | §4-3 准确率 ≥85% | 见 §4 非功能门槛 | ⬜ |
| US-05 | §4-5 执行过程可解释 | | ⬜ |

### 1.1 路由准确率测试用例集（编码前先写）

至少 20 条，覆盖已选的每个分类和场景，混入几条故意模糊 / 超纲的语句测试兜底：

| # | 输入语句 | 期望分类 | 期望回答要点 |
|---|---|---|---|
| 1 | | | |

## 2. 验证范围

本次验证覆盖 prd.md §2 / spec.md §3 选定的场景清单（列出实际测试跑过的场景，不是"应该测"的场景）：

## 3. 系统级回归确认（门禁二）

> 依赖 plan.md §5（高可用与降级设计），plan.md 确认后再填"期望降级行为"这一列。

| 注入的故障场景 | 期望降级行为 | 实际表现 | 通过 |
|---|---|---|---|
| 工具调用超时 | | | ⬜ |
| 工具调用报错 | | | ⬜ |

## 4. 非功能门槛

- 路由准确率（US-04）：正确数 / 总数 = ___ / ___ = ___%（门槛 ≥85%，是否达标：⬜）
- 性能（prd §2.2 的 10 QPS）：测试方法/工具： ；实测结果： ；是否达标：⬜
- 可观测性（US-05）：跑一遍 Golden Path，检查路由决策 / 智能体调用 / 工具调用参数与返回是否都能查到，且带时间戳：⬜

## 5. 不改清单核对（门禁三）

> 依赖 plan.md §6，plan.md 确认后把清单条目原样搬过来再核对，不要自己另编一份。

逐条核对 plan.md §6"不改清单"是否被破坏：

| 不改清单条目 | 是否被改动 |
|---|---|
| | ⬜ 未改动 / ⬜ 被改动（说明原因） |

## 6. 遗留与例外

- 未覆盖的场景 / 已知问题：

## 7. 判定结论

- 结论：通过 / 有条件通过 / 不通过
- 理由：

## 8. 变更记录

这份文件有两轮评审 + 一次编码后回填，每一轮的纠错和补充都要留痕——最后 learnings.md 会连同 §3/§4 未通过的门禁、§6 遗留问题、§7 判定理由一起读走：

| 时间 | 阶段 | 改动（纠错 / 补充了什么） | 人 |
|---|---|---|---|
| | Agent 起草 §0/§1/§2/§4 | 初稿（测试用例部分） | |
| | 人评审 §0/§1/§2/§4 | | |
| | Agent 补齐 §3/§5 | 初稿（降级测试 + 不改清单核对） | |
| | 人评审 §3/§5 | | |
| | 编码后实测回填 | | |
