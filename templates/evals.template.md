---
evalsId: EVALS-<组名>-001
type: evals
title: 企业级高可用多智能体客服 AI 智能体系统
owner: <组名 / 组员姓名>
status: 草稿          # 草稿（编码前先定用例）→ 已实测（编码后回填结果）
based_on: [spec.md, prd.md]
created_at:
related: [learnings.md]
---

<!-- ──────────────────────────────────────────────────────────────
  evals.md ｜ 评测验收（先定标准，再编码；编码后回填实测结果）

  这一步要做什么      这份文件分两次写：
        第一次（编码开始前）：把 spec.md §4 里"怎么测"的口径，落成
        具体的测试用例集——这一步要在编码前做，相当于给 Coding Agent
        一个"验收靶子"，而不是代码写完了才现想测什么。
        第二次（编码完成后）：跑一遍测试用例，把实测结果填进来，
        对照 prd.md §4 的验收线打勾。

  为什么先定用例再编码      没有先定好的验收标准，"做完了"就只能靠
        肉眼感觉判断，容易在演示环节才发现准确率不达标或某个场景
        没兜住。
────────────────────────────────────────────────────────────── -->

# EVALS-\<组名\>-001｜评测验收

## 1. 路由准确率测试用例集（编码前先写）

至少 20 条，覆盖已选的每个分类和场景，混入几条故意模糊 / 超纲的语句测试兜底：

| # | 输入语句 | 期望分类 | 期望回答要点 |
|---|---|---|---|
| 1 | | | |

## 2. 路由准确率 —— 实测结果（编码后回填）

- 测试方法：
- 实测：正确数 / 总数 = ___ / ___ = ___%
- 是否达到 ≥85% 门槛：是 / 否
- 未命中用例分析：

## 3. 性能测试（10 QPS）—— 实测结果

- 测试方法 / 工具：
- 实测结果：
- 是否达标：是 / 否

## 4. 高可用测试 —— 实测结果

| 注入的故障场景 | 期望降级行为 | 实际表现 | 通过 |
|---|---|---|---|
| 工具调用超时 | | | |
| 工具调用报错 | | | |

## 5. 已知问题 / 未覆盖场景清单

-
